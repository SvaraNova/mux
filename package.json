{
  "name": "mux-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "mux - Collaborative AI Development TUI",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run",
    "mux": "tsx apps/cli/src/bin/mux.ts"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
